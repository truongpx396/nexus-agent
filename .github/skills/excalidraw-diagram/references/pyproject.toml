[project]
name = "nexus-agent-excalidraw-renderer"
version = "0.1.0"
description = "Local renderer for validating Excalidraw files used by the workspace skill"
requires-python = ">=3.11"
dependencies = [
  "playwright>=1.54.0"
]

[tool.uv]
package = false